# Optional AI explanation. Keep real values in the process environment or an ignored
# local .env workflow; never commit credentials. SerpentGuard does not auto-load this
# example file.
OPENAI_API_KEY=
SERPENTGUARD_OPENAI_MODEL=
SERPENTGUARD_OPENAI_TIMEOUT_SECONDS=30
